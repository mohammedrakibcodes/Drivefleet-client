import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login | DriveFleet",
  description: "Login to your DriveFleet account",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
